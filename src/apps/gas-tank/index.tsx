import { AppWrapper } from './components/App/AppWrapper';
import '../pulse/styles/tailwindPulse.css';

/**
 * Gas Tank App Entry Point
 * Main component exported for app loader
 */
export default AppWrapper;
